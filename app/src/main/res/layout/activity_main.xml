<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="true"
    android:background="@android:color/black"
    android:clipToPadding="false"
    tools:context="co.mide.circlevideo.MainActivity">

    <TextureView android:id="@+id/texture_view"
        android:layout_margin="0dp"
        android:padding="0dp"
        android:clickable="true"
        android:layout_centerInParent="true"
        android:layout_height="match_parent"
        android:layout_width="match_parent"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:gravity="center|bottom"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:layout_height="wrap_content">
        <ImageButton
            android:padding="10dp"
            android:alpha="0.8"
            android:contentDescription="@string/switch_camera_button_description"
            android:id="@+id/image_button_switch"
            android:layout_margin="20dp"
            android:background="?android:attr/selectableItemBackground"
            android:src="@drawable/ic_switch_camera_white_24dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
        <co.mide.circlevideo.RecordButton
            android:elevation="5dp"
            android:contentDescription="@string/shutter_button_description"
            android:id="@+id/record_button"
            android:background="@drawable/drawable_shutter_button"
            android:layout_width="100dp"
            android:padding="10dp"
            android:layout_margin="5dp"
            android:layout_height="100dp"/>
        <ImageButton
            android:contentDescription="@string/flash_button_description"
            android:padding="10dp"
            android:id="@+id/image_button_flash"
            android:visibility="invisible"
            android:alpha="0.8"
            android:layout_margin="20dp"
            android:background="?android:attr/selectableItemBackground"
            android:src="@drawable/ic_flash_auto_white_24dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </LinearLayout>
</RelativeLayout>